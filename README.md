# Mail2Receipt

This is a static browser application. Selected email files are processed only in browser memory and never leave the browser.

Only HTML email is accepted. Remote images are blocked by default and can be enabled only after an explicit warning for the active file.
